<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/OffWhite" /> <!-- Background color -->
    <corners android:radius="20dp" /> <!-- Adjust corner radius as needed -->
    <stroke
        android:width="2dp"
    android:color="@color/Dark" />
</shape>
