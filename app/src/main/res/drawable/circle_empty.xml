<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" /> <!-- Transparent color for the circle -->
    <stroke
        android:width=".25dp"
    android:color="@color/Dark" />
    <size
        android:width="20dp"
    android:height="20dp" />
</shape>
